var num
var x

: 0= 0 = ;
: T4 x @ 0=
	if drop 'y' emit exit then
	drop 'n' emit ;

var x
: Mil ( n--m ) 1000 dup * * ;
: CR 10 emit ;

: main
	T4
	1000 Mil x !
	x @ .d
	's' 'e' swap emit
	x @ begin 1- while drop
	emit CR // $41 emit
	" - all done!" puts
;
